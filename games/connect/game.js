const Discord = require("discord.js");

const players = {
	EMPTY:0,
	RED:1,
	YELLOW:2
};

class game {
	/**
	 * Create 
	 * @param {Discord.Message} message 
	 */
	constructor(message){
		this.message = message;
	}
	
	swithcTurn()
}